<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">

        <item
            android:id="@+id/menu_nav_home"
            android:checked="true"
            android:icon="@drawable/ic_home"
            android:title="@string/menu_home" />

        <item
            android:id="@+id/menu_nav_live"
            android:checked="false"
            android:icon="@drawable/ic_movie"
            android:title="@string/menu_live" />

        <item
            android:id="@+id/menu_nav_highlight"
            android:checked="false"
            android:icon="@drawable/ic_series"
            android:title="@string/menu_highlight" />

        <item
            android:id="@+id/menu_nav_channel"
            android:checked="false"
            android:icon="@drawable/ic_channel"
            android:title="@string/menu_channel" />


    </group>
</menu>